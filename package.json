{
  "name": "mining-claim-locator",
  "version": "2.0.0",
  "description": "Interactive app to locate expired mining claims in Arizona using official BLM open data and Mapbox GL JS",
  "scripts": {
    "start:backend": "cd backend && node server.js",
    "start:frontend": "cd frontend && npm start",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm run test:ci",
    "test": "npm run test:backend && npm run test:frontend",
    "build:frontend": "cd frontend && npm run build",
    "setup:db": "psql -U postgres -c 'CREATE DATABASE mining_claims;' && psql -U postgres -d mining_claims -f scripts/setup_database.sql",
    "fetch:data": "cd scripts && node fetch_blm_data.js",
    "fetch:data:py": "cd scripts && python3 blm_fetcher.py"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
